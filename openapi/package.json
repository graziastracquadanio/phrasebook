{
  "name": "openapi",
  "version": "1.0.0",
  "scripts": {
    "bundle": "redocly lint src/openapi.yaml && redocly bundle src/openapi.yaml --output dist/bundle.yaml",
    "build-docs": "npm run bundle && redocly build-docs dist/bundle.yaml --output dist/index.html",
    "pre-commit": "npx lint-staged"
  },
  "lint-staged": {
    "src/**/*.yaml": "redocly lint"
  },
  "devDependencies": {
    "@redocly/cli": "^1.12.2"
  }
}
